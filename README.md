# fedora-kde-setup

This is a repository containing installation scripts for a new Fedora KDE
device.

This is a work in progress so probably not up to date.

We have the following:

setup.sh
core/
gaming/

Running setup.sh will prompt the user for which categories should be downloaded.

